
    Здесь вкратце дается обзор предлагаемого решения по тестовому заданию 
https://shiftproject.ru/resources/uploads/SHIFT_Java_Test_Task_f8f56f0c1e.pdf

   Пользовался версией java 25.0.2 2026-01-20 LTS, взятой на сайте 
https://www.oracle.com/asean/java/technologies/downloads/. Одной из фич данной версии 
является возможность запускать исходник на исполнение в командной строке:

    java Program.java

без использования редакторов типа NetBeans и явного запуска компиляторов
типа javac и использования систем сборок типа Maven. Если запустить нашу 
программу filter.java без аргументов, то на экран выведется:

Error in arguments. Usage: java filter.java <arguments>
Where <arguments> is one or few of:
         -o <path_to_folder> to use folder for output files
         -p <prefix> to use prefix for output filenames
         -[s|f] to display small or full statistic
         -a to use append Mode for output files
         <files> to use list of input files
By example: java filter.java -o subfolder in1.txt in2.txt

Ну хорошо, в соответвии со стандартом скомпилил jar-файл командой 
    jar cf filter.jar filter.class 
(а также вручную добавил в манифест строку Main-Class: filter) и его 
также можно запустить командой java -jar filter.jar и получим почти такой же ответ

Error in arguments. Usage: java -jar filter.jar <arguments>
Where <arguments> is one or few of:
         -o <path_to_folder> to use folder for output files
         -p <prefix> to use prefix for output filenames
         -[s|f] to display small or full statistic
         -a to use append Mode for output files
         <files> to use list of input files
By example: java -jar filter.jar -o subfolder in1.txt in2.txt

Высылаю исходник и jar-файл вместе с данной инструкцией. Спасибо за интерес 
к данной работе.

P.S. Конечно, могут появиться замечания типа отказа от продолжения работы программы 
при попытке создать файл в несуществующей папке, но я придерживался технического задания,
где не говорилось об обязательном создании несуществующих папок.
